#include<stdio.h>
int main(){
float l,b,p;
printf("\nEnter the Length and the breadth of the rectangle\n");
scanf("%f%f",&l,&b);
p=2*(l+b);
printf("\nPerimeter of the Rectangle is %f units\n",p);
return(0);
}
